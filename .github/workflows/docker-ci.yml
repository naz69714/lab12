name: CI/CD + Security (Docker optional)

on:
  push:
    branches: [ "main" ]

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Кодты тексеру мысалы
      - name: Run simple tests
        run: echo "Tests passed"

      # Trivy security scan (Docker image жоқ болса, тек мысал ретінде)
      - name: Trivy security scan
        run: echo "Trivy scan skipped (no Docker installed)"
